# Copy to .env. Vite only exposes variables that start with VITE_, and bakes them into the bundle at build time.
# Local: the server's PORT from server/.env. Production: your deployed API, e.g. https://your-api.onrender.com/api/v1
VITE_API_URL=http://localhost:5000/api/v1
